import React from 'react';
import './App.css';
import Child from './Child'
function Buttons (props) {

    return (
        <h2>here is the value for normal counter : {props.counter}</h2>
       
    )
}

export default Buttons